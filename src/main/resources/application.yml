spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3307/user_system?characterEncoding=utf-8&useSSL=false&useUnicode=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
